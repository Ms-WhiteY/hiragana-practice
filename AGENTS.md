# AGENTS.md

## 適用範囲

この指示は、リポジトリ全体に適用する。

## プロジェクト概要

このリポジトリは、4歳児向けの「ひらがな書き順なぞり練習Webアプリ」である。

- 公開先: GitHub Pages
- 公開元: `main` ブランチ
- 主端末: iPad Safari
- 対応端末: Fireタブレット、PCブラウザ
- 対象文字: あ、い、う、え、お、か、き、く、け、こ、さ、し、す、せ、そ
- 外部API、広告、アクセス解析、ログイン機能なし
- 星の数だけをブラウザの `localStorage` に保存
- PWA・オフライン利用対応
- KanjiVG由来のSVG書き順データを使用

最優先事項は「4歳児が一人でも迷わず使えること」と「既存機能を壊さないこと」である。

## 現在の構成

ビルド工程やフレームワークを持たない静的Webアプリである。

- `index.html`
  - HTML、CSS、JavaScriptを内包
  - ひらがなSVGパス、描画、判定、音声、星、UIを実装
- `sw.js`
  - PWAのオフラインキャッシュを管理
- `manifest.webmanifest`
  - PWA設定
- `icon-192.png` / `icon-512.png`
  - PWAアイコン
- `README.md`
  - 仕様、使い方、プライバシー、ライセンス
- `NOTICE_KANJIVG.txt`
  - KanjiVGの帰属・ライセンス表記

大きな構成変更やフレームワーク導入は、明示的に依頼された場合のみ行う。

## UI・UX方針

- 文字をまだ十分に読めない4歳児を基準に設計する。
- 説明文だけに依存せず、色、形、矢印、音声で操作を理解できるようにする。
- 緑は始点、オレンジは進行方向と終点、青は利用者の筆跡として維持する。
- タップ対象は幼児が押しやすい大きさと間隔を保つ。
- 重要な操作を小さな文字や複雑なメニューに隠さない。
- 音声が使えない環境でも基本操作を続けられるようにする。
- iPadのセーフエリア、縦横表示、タッチスクロールとの競合に注意する。
- 見た目だけを理由に、幼児向けの分かりやすさや操作性を低下させない。

## ブラウザ・入力対応

iPad Safariでの安定動作を最優先する。

- 指、Apple Pencil、一般的なタッチペン、マウスを考慮する。
- Pointer Events、pointer capture、`touch-action` の挙動を不用意に変更しない。
- 描画中のページスクロール、選択、拡大操作との競合を確認する。
- Safariで不安定または対応状況が不明な新しいAPIへ安易に依存しない。
- Service WorkerはHTTPSまたはローカルHTTPサーバーで確認する。`file://` だけでPWA動作を判断しない。

## v5判定ロジックの保護

現在のv5には、交差する長い画を幼児が正しくなぞっても逆向きと誤判定しにくくする重要な修正が含まれている。

特に次を保護する。

- `addMarkers()` による、長さに応じた複数の方向矢印
- `nearestProgressAware()` による、現在の進捗付近に限定した最近傍探索
- 一瞬の交差や手ぶれだけでは逆向きと判定しない処理
- 継続した大きな逆行だけを逆向きとして扱う処理
- 幼児向けの甘めな始点、経路、進捗、終点判定
- KanjiVG由来の各文字・各画のSVGパス

明示的な依頼がない限り、次を変更しない。

- SVGパスデータ
- サンプリング方法やサンプル数
- 探索範囲
- 始点・終点の許容距離
- はみ出し許容率
- 進捗判定値
- 逆行回数・逆行率のしきい値

判定ロジックを変更する場合は、変更理由、影響する文字・画、厳しくなるか甘くなるかを説明する。

## 必須の回帰確認

判定、描画、SVGパス、Pointer Eventsに関係する変更では、少なくとも以下を確認する。

### 最重要ケース

1. 「あ」の3画目を正しい方向になぞり、成功すること
2. 「お」の2画目を正しい方向になぞり、成功すること
3. 上記の交差部分で一瞬位置がぶれても、逆向きと誤判定されないこと
4. 明確に逆方向へ継続してなぞった場合は、逆向きとして扱われること

### 基本機能

- あ、い、う、え、おを選択できる
- 各画が正しい順番で進む
- 緑の始点、オレンジの矢印・終点が表示される
- 長い画には複数の方向矢印が表示される
- 「おてほん」アニメーションが最後まで動く
- 「なぞる」と「じぶんで」を切り替えられる
- 「もういちど」と「けす」が動く
- 「🔊 きく」が利用可能な環境で動く
- 成功時に星が増える
- 再読み込み後も星が保持される
- 既存の星がバージョン更新で失われない

### 表示・PWA

- iPad相当の画面幅で操作部分が切れない
- 小さい画面とPC幅でもレイアウトが破綻しない
- オンラインで初回表示できる
- Service Worker導入後にオフライン再読み込みできる
- 更新後に新しいキャッシュへ切り替わる

実機確認ができない場合は、その旨と未確認範囲を明記する。

## バージョンとキャッシュ

アプリバージョンを一元管理する仕組みを導入する場合は、画面表示とService Workerのキャッシュ名が同じ値から生成されるようにする。

- バージョン文字列の管理箇所を1か所にする。
- `app-version.js` 導入後は、そのファイルをアプリ表示バージョンの唯一の正とし、他ファイルへ同じバージョン番号を手書きで重複させない。
- バージョン更新だけのために複数ファイルの数値を手作業で合わせる構成を避ける。
- キャッシュ名にはアプリ固有の接頭辞を付ける。
- 旧キャッシュ削除は、このアプリの接頭辞を持つキャッシュだけを対象にする。
- 同一オリジン上の別アプリのキャッシュを削除しない。
- iPad Safariで更新を検知できる構成を優先する。

`hiragana-stars-v3` は保存データ形式のキーであり、アプリ表示バージョンではない。単なるリリース番号変更に合わせて変更してはならない。

## KanjiVGとライセンス

KanjiVG由来データの帰属表示とライセンス情報を必ず維持する。

保護対象:

- 画面フッターのKanjiVG表記
- `index.html` 内のライセンスコメント
- `README.md` の書き順データ・ライセンス説明
- `NOTICE_KANJIVG.txt`

KanjiVG由来のパスを変更・追加する場合は、Creative Commons Attribution-ShareAlike 3.0の条件を確認する。

## プライバシーと安全性

明示的な依頼がない限り、次を追加しない。

- 子どもの氏名、写真、音声などの個人情報
- アカウントやログイン
- サーバーへの学習履歴送信
- 広告
- アクセス解析
- 外部API
- トラッキングCookie
- 不要な外部CDNや外部ライブラリ

保存が必要な情報は、可能な限り端末内に限定する。

## 変更時の進め方

1. 作業前に現在の `main`、作業ツリー、対象ファイルを確認する。
2. 既存の変更や未コミット内容を上書きしない。
3. 関係するコードを読んでから修正する。
4. 目的を満たす最小限の差分にする。
5. `index.html` 全体の全面書き換えや無関係な整形を避ける。
6. 既存のUI、判定、音声、星、PWA機能を維持する。
7. 可能な範囲で動作確認と回帰確認を行う。
8. 変更ファイル、確認内容、未確認事項を作業完了時に報告する。

依頼されていないデプロイ、公開、push、履歴の書き換えは行わない。

`main` へのpushは、ユーザーから明示的に依頼された場合のみ行う。変更内容の確認だけを依頼された場合はpushしない。

## コミット

コミットを依頼された場合は、変更内容が分かる具体的なメッセージを使用する。

例:

- `Show app version and unify cache versioning`
- `Fix direction tracking for crossing strokes`
- `Preserve offline cache during app version update`

大きく異なる変更を1つのコミットに混在させない。

